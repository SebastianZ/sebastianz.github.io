request.counter = 0;

start = getTickCount();
for (i = 1; i <= 20; i++) {
	thread {
		sleep(1000);
		request.counter++;
	}
}
threadJoin();
dump(var=request.counter, label="All threads completed");
dump("Total execution time: #getTickCount()-start#ms");